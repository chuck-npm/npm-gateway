<?php
declare(strict_types=1);
use NpmGateway\Container\ServiceProvider;
use NpmGateway\Contracts\ClockInterface;
use NpmGateway\Contracts\InitializationTransactionInterface;
use NpmGateway\Database\DatabaseProfiles;
use NpmGateway\Database\MySqlConnectionFactory;
use NpmGateway\Exceptions\Domain\ApplicationReviewAlreadyCompletedException;
use NpmGateway\Exceptions\Domain\CommunityActionPropertyForbiddenException;
use NpmGateway\Exceptions\Domain\InvalidApplicationReviewException;
use NpmGateway\Http\AuthenticatedRequestContext;
use NpmGateway\Http\Controllers\CorporateApplicationReviewController;
use NpmGateway\Http\Request;
use NpmGateway\Notifications\ApplicationReviewEmailSender;
use NpmGateway\Repositories\ApplicationReviewRepository;
use NpmGateway\Repositories\PropertyAccessRepository;
use NpmGateway\Services\ApplicationReviewService;
use NpmGateway\Services\ApplicationReviewQueryService;
use NpmGateway\Services\ApplicationReviewValidator;
use NpmGateway\Services\CorporateAccessService;
use NpmGateway\Services\AuditService;
use NpmGateway\Services\CommunityActionContextResolver;
use NpmGateway\Support\PublicIdGenerator;
use NpmGateway\Support\FlashSession;
use NpmGateway\Security\CsrfService;
use NpmGateway\Contracts\CorporateToolsProviderInterface;
use NpmGateway\ValueObjects\AuthenticatedUser;
$app=require dirname(__DIR__,2).'/bootstrap/app.php';foreach(['application','migration'] as $profile)if(DatabaseProfiles::load($profile,$app['root'])['database']!=='npmgateway_test'){fwrite(STDERR,"Application Reviews disposable profile gate failed.\n");exit(2);}$db=MySqlConnectionFactory::connect(DatabaseProfiles::load('migration',$app['root']));$mode=$argv[1]??'';$ids=new PublicIdGenerator();
$clean=static function(mysqli $db):void{foreach(['application_review_history','application_reviews','user_property_access','user_category_access','audit_logs','employee_property_assignments','notification_recipients','notifications','user_sessions','login_attempts','properties','users','employees'] as $table){if((int)$db->query("SELECT COUNT(*) FROM information_schema.TABLES WHERE TABLE_SCHEMA=DATABASE() AND TABLE_NAME='{$table}'")->fetch_row()[0])$db->query("DELETE FROM {$table}");}};
$person=static function(mysqli $db,PublicIdGenerator $ids,string $first,string $last,string $class,string $number,string $username):array{$ep=$ids->generate();$s=$db->prepare("INSERT INTO employees(public_id,employee_number,employee_class,first_name,last_name,job_title,employment_status,start_date) VALUES(?,?,?,?,?,'Disposable Review','active','2026-08-05')");$s->bind_param('sssss',$ep,$number,$class,$first,$last);$s->execute();$eid=$db->insert_id;$s->close();$up=$ids->generate();$hash=password_hash('Disposable-123!',PASSWORD_DEFAULT);$s=$db->prepare("INSERT INTO users(public_id,employee_id,username,password_hash,status) VALUES(?,?,?,?,'active')");$s->bind_param('siss',$up,$eid,$username,$hash);$s->execute();$uid=$db->insert_id;$s->close();$user=new AuthenticatedUser($uid,$eid,$up,$ep,$username,$first.' '.$last,'Disposable Review',$class);return ['user'=>$user,'context'=>new AuthenticatedRequestContext($user,'token')];};
$property=static function(mysqli $db,PublicIdGenerator $ids,string $code,string $slug,string $name,int $propId):array{$public=$ids->generate();$mail=$slug.'@example.test';$s=$db->prepare("INSERT INTO properties(public_id,prop_id,property_code,slug,display_name,status,manager_email,address_line_1,city,state,postal_code,timezone) VALUES(?,?,?,?,?,'active',?,'1 Test Way','Test','PA','18503','America/New_York')");$s->bind_param('sissss',$public,$propId,$code,$slug,$name,$mail);$s->execute();$id=$db->insert_id;$s->close();return ['id'=>$id,'public_id'=>$public,'slug'=>$slug];};
try{
 if($mode==='certify'){
  $clean($db);$manager=$person($db,$ids,'Review','Manager','manager','NPM979001','reviewmanager');$reviewer=$person($db,$ids,'Corporate','Reviewer','corporate','NPM979002','reviewcorporate');$alpha=$property($db,$ids,'AR','review-alpha','Review Alpha',979001);$beta=$property($db,$ids,'BR','review-beta','Review Beta',979002);$at='2026-08-05 09:00:00';$access=new PropertyAccessRepository($db);$access->grant(['public_id'=>$ids->generate(),'user_id'=>$manager['user']->id,'property_id'=>$alpha['id'],'granted_by_user_id'=>$reviewer['user']->id,'granted_at'=>$at]);$s=$db->prepare("INSERT INTO user_category_access(public_id,user_id,category,granted_by_user_id,granted_at) VALUES(?,?,'application-reviews',?,?)");$category=$ids->generate();$reviewerId=$reviewer['user']->id;$s->bind_param('siis',$category,$reviewerId,$reviewerId,$at);$s->execute();$s->close();$container=ServiceProvider::build($app);$resolver=$container->get(CommunityActionContextResolver::class);$trusted=$resolver->resolve($manager['context'],$alpha['slug']);try{$resolver->resolve($manager['context'],$beta['slug']);throw new RuntimeException('Unauthorized property resolved.');}catch(CommunityActionPropertyForbiddenException){}
  $deliveries=[];$email=new ApplicationReviewEmailSender(['test_mode'=>true,'test_email'=>'chuck.application-review@example.test','app_url'=>'https://gateway.example.test'],function(string $to,string $subject,string $html,string $text)use(&$deliveries):bool{$deliveries[]=compact('to','subject','html','text');return true;});$repo=new ApplicationReviewRepository($db);$service=new ApplicationReviewService($repo,new ApplicationReviewValidator(),$container->get(InitializationTransactionInterface::class),$ids,$container->get(ClockInterface::class),$container->get(AuditService::class),$email,$container->get(\NpmGateway\Services\PropertyAccessService::class));$session=[];$csrf=new CsrfService($session);$token=$csrf->token();$controller=new CorporateApplicationReviewController($container->get(CorporateAccessService::class),new ApplicationReviewQueryService($repo),$service,$container->get(CorporateToolsProviderInterface::class),$csrf,new FlashSession($session),$app['root'].'/resources/views');
  $first=$service->submit($trusted,['prospect_name'=>'First Prospect','manager_comments'=>'Manager basis','rm_documents_confirmed'=>'confirmed','property_id'=>$beta['id'],'user_id'=>$reviewer['user']->id,'recipient'=>'attacker@example.test']);$one=$repo->corporateDetail($first['public_id']);if($one===null||(int)$one['property_id']!==$alpha['id']||(int)$one['submitted_by_user_id']!==$manager['user']->id||$one['status']!=='pending_review'||$one['reviewed_by_user_id']!==null||count($one['history'])!==1)throw new RuntimeException('Trusted submission persistence failed.');if(count($deliveries)!==1||$deliveries[0]['to']!=='chuck.application-review@example.test')throw new RuntimeException('Submission test routing failed.');if(count($repo->corporateQueue())!==1)throw new RuntimeException('Corporate queue failed without Property Access.');$rendered=$controller->show($first['public_id'],$reviewer['context']);$dom=new DOMDocument();@$dom->loadHTML($rendered->body);$xpath=new DOMXPath($dom);$forms=$xpath->query('//form[@data-processing-form]');$approve=$xpath->query('//form[@data-processing-form]//button[@type="submit" and @name="decision" and @value="approved"]');$deny=$xpath->query('//form[@data-processing-form]//button[@type="submit" and @name="decision" and @value="denied"]');$notes=$xpath->query('//form[@data-processing-form]//textarea[@name="reviewer_comments"]');$cancel=$xpath->query('//form[@data-processing-form]//a[normalize-space()="Cancel"]');if($forms->length!==1||$approve->length!==1||$deny->length!==1||$notes->length!==1||$cancel->length!==1||$xpath->query('//form//form')->length!==0)throw new RuntimeException('Rendered decision form contract failed.');$invalid=$controller->decide(new Request('POST','/corporate/application-reviews/'.$first['public_id'].'/decision',['_token'=>$token,'decision'=>'approved','reviewer_comments'=>'   ']),$first['public_id'],$reviewer['context']);if($invalid->status!==303)throw new RuntimeException('Invalid controller request did not redirect.');$invalidPage=$controller->show($first['public_id'],$reviewer['context']);$one=$repo->corporateDetail($first['public_id']);if(!str_contains($invalidPage->body,'Review Notes are required before approving or denying an application.')||$one['status']!=='pending_review'||count($one['history'])!==1||count($deliveries)!==1)throw new RuntimeException('Invalid controller request mutated state or hid feedback.');$approvalNotes='Approval test notes';$approved=$controller->decide(new Request('POST','/corporate/application-reviews/'.$first['public_id'].'/decision',['_token'=>$token,'decision'=>'approved','reviewer_comments'=>$approvalNotes]),$first['public_id'],$reviewer['context']);if($approved->status!==303)throw new RuntimeException('Approval controller request did not redirect.');$one=$repo->corporateDetail($first['public_id']);if($one['status']!=='approved'||$one['reviewer_comments']!==$approvalNotes||(int)$one['reviewed_by_user_id']!==$reviewer['user']->id||$one['reviewed_at']===null||count($one['history'])!==2||$one['history'][1]['event_type']!=='approved'||$one['history'][1]['comments']!==$approvalNotes||!str_contains($deliveries[1]['text'],$approvalNotes))throw new RuntimeException('Approval controller contract failed.');$duplicate=$controller->decide(new Request('POST','/corporate/application-reviews/'.$first['public_id'].'/decision',['_token'=>$token,'decision'=>'denied','reviewer_comments'=>'Overwrite']),$first['public_id'],$reviewer['context']);if($duplicate->status!==303)throw new RuntimeException('Second decision controller request did not redirect.');
  $second=$service->submit($trusted,['prospect_name'=>'Second Prospect','manager_comments'=>'','rm_documents_confirmed'=>'confirmed']);$denialNotes='Denial test notes';$denied=$controller->decide(new Request('POST','/corporate/application-reviews/'.$second['public_id'].'/decision',['_token'=>$token,'decision'=>'denied','reviewer_comments'=>$denialNotes]),$second['public_id'],$reviewer['context']);if($denied->status!==303)throw new RuntimeException('Denial controller request did not redirect.');$two=$repo->corporateDetail($second['public_id']);if($two['status']!=='denied'||$two['reviewer_comments']!==$denialNotes||$two['history'][1]['event_type']!=='denied'||$two['history'][1]['comments']!==$denialNotes||!str_contains($deliveries[3]['text'],$denialNotes))throw new RuntimeException('Denial controller contract failed.');$missingDecision=$service->submit($trusted,['prospect_name'=>'Missing Decision Prospect','manager_comments'=>'','rm_documents_confirmed'=>'confirmed']);$missing=$controller->decide(new Request('POST','/corporate/application-reviews/'.$missingDecision['public_id'].'/decision',['_token'=>$token,'reviewer_comments'=>'Valid notes']),$missingDecision['public_id'],$reviewer['context']);$missingPage=$controller->show($missingDecision['public_id'],$reviewer['context']);$missingRecord=$repo->corporateDetail($missingDecision['public_id']);if($missing->status!==303||!str_contains($missingPage->body,'Choose Approve or Deny.')||$missingRecord['status']!=='pending_review'||count($missingRecord['history'])!==1)throw new RuntimeException('Missing decision validation contract failed.');if(count($deliveries)!==5||array_unique(array_column($deliveries,'to'))!==['chuck.application-review@example.test'])throw new RuntimeException('Decision test routing failed.');$failedEmail=new ApplicationReviewEmailSender(['test_mode'=>true,'test_email'=>'chuck.application-review@example.test','app_url'=>'https://gateway.example.test'],fn():bool=>false);$failureService=new ApplicationReviewService($repo,new ApplicationReviewValidator(),$container->get(InitializationTransactionInterface::class),$ids,$container->get(ClockInterface::class),$container->get(AuditService::class),$failedEmail,$container->get(\NpmGateway\Services\PropertyAccessService::class));$failed=$failureService->submit($trusted,['prospect_name'=>'Failure Prospect','manager_comments'=>'','rm_documents_confirmed'=>'confirmed']);if($failed['email_sent']!==false||count(($repo->corporateDetail($failed['public_id']))['history']??[])!==1)throw new RuntimeException('Email failure did not preserve one committed review/history.');$access->revoke($manager['user']->id,$alpha['id']);try{$resolver->resolve($manager['context'],$alpha['slug']);throw new RuntimeException('Revoked manager retained access.');}catch(CommunityActionPropertyForbiddenException){}if($repo->corporateDetail($first['public_id'])===null)throw new RuntimeException('Corporate history disappeared after revoke.');if((int)$db->query('SELECT COUNT(*) FROM notifications')->fetch_row()[0]!==0||(int)$db->query('SELECT COUNT(*) FROM notification_recipients')->fetch_row()[0]!==0)throw new RuntimeException('Notification rows were created.');$audit=json_encode($db->query("SELECT event_type,after_data FROM audit_logs WHERE event_type LIKE 'application_review.%'")->fetch_all(MYSQLI_ASSOC),JSON_THROW_ON_ERROR);foreach(['First Prospect','Second Prospect','Missing Decision Prospect','Failure Prospect','Manager basis',$approvalNotes,$denialNotes,'@example.test'] as $private)if(str_contains($audit,$private))throw new RuntimeException('Private content leaked to audit.');echo "profiles=npmgateway_test\nrendered_form_contract=passed\ntrusted_property_user=passed\nsubmitted_history=1\ncorporate_without_property_access=passed\napprove_controller_303=passed\ndeny_controller_303=passed\napproved_record_history_email_notes=consistent\ndenied_record_history_email_notes=consistent\ncomments_required_no_mutation=passed\nmissing_decision_visible_error=passed\nsecond_decision=rejected\nmanager_revoke=immediate_403\ncorporate_history=retained\ntest_emails=5_chuck_only\nemail_failure=record_and_single_history_preserved\nnotifications=0\naudits=sanitized\n";
 }elseif($mode==='cleanup'){$clean($db);echo "fixtures_cleaned=yes\n";}elseif($mode==='residue'){$total=0;foreach(['application_review_history','application_reviews','user_property_access','user_category_access','audit_logs','employee_property_assignments','notification_recipients','notifications','properties','users','employees'] as $table)$total+=(int)$db->query("SELECT COUNT(*) FROM {$table}")->fetch_row()[0];echo "disposable_residue={$total}\n";if($total!==0)exit(1);}else throw new InvalidArgumentException('Unknown mode.');
}catch(Throwable $e){fwrite(STDERR,str_replace(["\r","\n"],' ',$e->getMessage())."\n");exit(1);}finally{$db->close();}
